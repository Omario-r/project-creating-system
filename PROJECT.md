# PROJECT.md — оверлей проекта

> Проектные адаптации поверх генерик-правил из `system/`. Читается в любой роли
> вместе с правилами роли. Генерик-файлы (`system/`) локально НЕ редактируются —
> всё проектное живёт здесь. Модель синхронизации — `METHODOLOGY.md`.
>
> Это заготовка. Заполни под свой проект; значения ниже — дефолты шаблона.

## Стек и структура

- **Каталог кода** (`<каталог кода>` в правилах): `src/`
- **Платформа:** <iOS/Android · web · desktop · CLI · ...>
- **Язык / фреймворк:** <...>

## Дизайн

> Читает Дизайнер (`system/DIZAINER.md`): платформа (выше) — чей корпус
> конвенций, класс приложения — режим их применения.

- **Класс приложения:** <стандартное приложение / игра> — стандартное =
  платформенные паттерны по умолчанию, отступления явно; игра = кастомный
  визуальный язык, конвенции только в системной зоне (safe area, жесты).
- **Целевые устройства / брейкпоинты:** <напр. телефоны 390×844..430×932,
  портрет; или веб: 360 / 768 / 1280>
- **Особенности:** <ориентация, тёмная/светлая тема, ограничения читаемости;
  если нет — «нет»>

## Гейт качества

- **Линтер** (DoD п. 1): `<команда>` — ноль ошибок и предупреждений.
- **Тесты:** `<команда запуска тестов>`
- **Смоук-тест / запуск проекта:** `<команда>`

Эти же команды продублируй в allowlist `.claude/settings.json`
(`permissions.allow`), если их нет в преднаборе, — иначе каждая сессия будет
останавливаться вопросом «разрешить команду?». Политика тиров — METHODOLOGY.md
шаблона (решение 2026-07-19); ослабление deny (секреты) — карве-аут здесь +
`.claude/settings.local.json`.

## Ростер

Пока роли называются словом (Постановщик / Исполнитель / флаг «тяжёлая»).
Конкретные субагенты и модели за ролями — Фаза 3 (`system/POSTANOVSHCHIK.md` § 4).

## Карве-ауты правил

- <проектные исключения или дополнения к генерик-правилам; если нет — «нет»>

## Синхронизация

- Синхронизировано с шаблоном @ `<commit>` (`<дата>`).
