# DESIGN — визуальная конституция проекта

> Владеет Дизайнер (`system/DIZAINER.md`), утверждает PO. Единственный источник
> визуальных значений: Исполнитель берёт цвета/размеры/отступы ТОЛЬКО отсюда и
> из мокапов; значения нет — эскалация Постановщику, не изобретать.
>
> Мокапы экранов — `docs/design/<экран>.html`, один файл = один экран
> (каркас — `system/templates/screen-mockup.html`).

## Подход

<Выбранный визуальный подход проекта: стиль, настроение, источники. 2–5 строк.
Фиксируется на первой дизайн-сессии. Платформа и класс приложения (режим
конвенций) — в `PROJECT.md`.>

## Токены

> Канон значений. Новый токен добавляется сюда дизайн-сессией, не локальной
> константой в мокапе или коде. Значение в коде разошлось с таблицей — это баг
> (строка в `docs/BACKLOG.md`).

| Токен | Значение | Где в коде | Комментарий |
|---|---|---|---|
| `<имя>` | `<hsl/hex/px>` | `<файл: константа>` / — | <...> |

## Язык форм и правила

- <радиусы, сетка отступов, типографика, иконки, движение/анимации — что
  канонично; если пока нет — «нет»>

## Отступления от конвенций платформы

> Итоги конвенция-чека (`system/DIZAINER.md` § 2). Каждая строка — явное
> решение PO.

- <ГГГГ-ММ-ДД — от какого паттерна отступаем и почему; если нет — «нет»>

## Индекс экранов

> Статусы ведёт Дизайнер. «Утверждён PO» = канон: реализация сверяется с
> мокапом на Гейте 2, отступление — продуктовый вопрос к PO. Эпик с видимым UI
> не декомпозируется, пока его экраны не «утверждён PO»
> (`POSTANOVSHCHIK.md` § 3).

| Экран | Файл | Статус | Обновлён |
|---|---|---|---|
| <имя> | `docs/design/<экран>.html` | черновик / утверждён PO | ГГГГ-ММ-ДД |
