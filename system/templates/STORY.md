# STORY <номер> — <имя стори>

> Заполняет Постановщик при декомпозиции эпика. PO утверждает на Гейте 1 и
> принимает на Гейте 2. Работа по стори НЕ начинается, пока секция «Открытые
> вопросы к PO» не пуста.
>
> Принцип контракта: **стори самодостаточна для нарезки задач, задача
> самодостаточна для написания кода.** Исполнитель задачи может быть другим
> (возможно, более простым) агентом — поэтому ТЗ каждой задачи должно быть
> полным, без опоры на контекст сессии Постановщика.

**Эпик:** <имя эпика>
**Статус:** не начата / в работе / на приёмке (Гейт 2) / возвращена (Гейт 2) / закрыта

## Ценность

Пользователь может **<X>**, чтобы **<Y>**.

## Критерии приёмки

> Проверяемые, конечный список. Именно по ним PO принимает стори на Гейте 2.
> Перед сдачей агент сам проходит каждый критерий: галочка + доказательство
> («запустил X → увидел Y»). PO проверяет выборочно.

- [ ] <критерий 1 — формулировка «сделай так — увидишь это»>
- [ ] <критерий 2>

## Не входит

- <явные границы: смежное поведение, которое НЕ реализуется в этой стори>

## Контекст для нарезки

> Всё, что нужно, чтобы нарезать и выполнять задачи, не перечитывая полпроекта:
> существующее поведение, которого касаемся; ограничения; зависимости от других
> стори/эпиков. Делает стори самодостаточной для Постановщика. Если пусто — «нет».

- <...>

## Затронутые модули

> По карте из ARCHITECTURE.md. Если модуля ещё нет — пометить «(новый)».

- `<каталог кода>/<модуль>/` — <зачем затрагивается>

## Открытые вопросы к PO

> Продуктовые вопросы (см. AGENT.md п. 2). Формат: вопрос → варианты →
> рекомендация. Пустая секция = можно работать.

_нет_

## Задачи

> Нарезает Постановщик. Каждая задача — самодостаточное ТЗ: одна сессия,
> не более трёх модулей, проверяемый результат, проект остаётся рабочим после
> каждой. Порядок = порядок выполнения (с учётом зависимостей).

### T1 — <имя задачи>
- **Статус:** не начата / в работе / готова
- **Рекомендованный агент:** <роль> · <бэкенд> — роль: Исполнитель / Исполнитель
  (тяжёлая); бэкенд по умолчанию Claude нужного тира (Sonnet / Opus), внешний
  (Cursor, Gigacode) — по решению PO или при лимитах; для внешнего добавь
  «сдача — `system/HANDOFF.md`» и см. требования к ТЗ в `POSTANOVSHCHIK.md` § 4.
  Ростер: `system/ROSTER.md`.
- **Цель:** <что эта задача даёт, 1–2 строки>
- **Модули (≤ 3):** `<каталог кода>/<модуль>/` (паспорт: `<каталог кода>/<модуль>/README.md`) — <роль в задаче>
- **Что сделать:** <конкретная постановка: шаги/спека, достаточные для кода без
  широкого контекста. Если задаются или меняются интерфейсы — сигнатуры здесь.>
- **Арх-дельта (пишет Постановщик):** <готовая правка ARCHITECTURE.md/DECISIONS.md,
  если задача добавляет/удаляет модуль или связь между модулями либо принимает
  архитектурное решение (выбор из альтернатив с долгими последствиями). Исполнитель
  применяет её как есть в том же коммите. Если архитектуру не задевает — «нет».>
- **Дизайн-референс (пишет Постановщик):** <если задача трогает видимое: путь к
  утверждённому мокапу (`docs/design/<экран>.html`) + **имена элементов** из его
  каталога (`el-<имя>`), которые задача реализует/меняет, + конкретные токены из
  `docs/DESIGN.md`. «Как в мокапе» без имени элемента или визуальный критерий
  без ссылки на мокап/токен = неполное ТЗ. Для внешнего бэкенда значения токенов
  продублируй числами прямо здесь — он файлы проекта не читает.
  Не трогает видимое — «нет».>
- **Результат (как проверить):** детерминированное — что запустить (линтер/тесты)
  и какой результат. Визуальные/поведенческие проверки (скриншот, «читается как…»,
  прогон в браузере) — на оркестраторе/Гейте 2, не в DoD Исполнителя.
- **DoD задачи:** <что закрыть помимо кода: паспорта каких модулей обновить,
  какие тесты, применена ли арх-дельта. Общий чек-лист — AGENT.md п. 5.>
- **Промпт-запуск (ТОЛЬКО для внешнего бэкенда; Постановщик подставляет значения,
  PO копирует в чат агента как есть; для Claude-бэкенда поле убрать):**

  ```
  Прочитай файл system/HANDOFF.md и следуй его чек-листу сдачи.
  Выполни задачу T<N> — и только её — из файла docs/epics/<эпик>/stories/<стори>.md.
  ```

- **Заметки по выполнению:** <заполняется по факту: что важно знать следующей сессии>

### T2 — <имя задачи>
- ...
