# MODULE — <имя модуля>

> Паспорт модуля. Живёт в `<каталог кода>/<модуль>/README.md`. 30–50 строк, не
> больше. Это ЕДИНСТВЕННОЕ, что другие агенты знают о модуле, не читая код.
> Обновляется в той же сессии, где менялся интерфейс/зависимости/инварианты.

## Назначение

<1–2 предложения: за что отвечает модуль. Если не получается коротко —
модуль делает слишком много.>

## Публичный интерфейс

> Только то, чем пользуются снаружи. Сигнатуры + 1 строка смысла.
> Внутренние функции сюда не попадают.

- `<функция/класс/API>` — <что делает>

## Зависит от

- `<модуль>` — <что именно использует>
- _(или «ничего»)_

## От него зависят

- `<модуль>` — <через какой интерфейс>
- _(или «никто»)_

## Инварианты

> Правила, которые нельзя нарушать при любых изменениях. Это защита от
> агента будущей сессии, который не знает контекста.

- <пример: «состояние модуля изменяется только через методы X/Y, прямой
  доступ к полям запрещён»>

## Как проверить

<Команда/действие, чтобы убедиться, что модуль работает. Тесты, если есть.>
