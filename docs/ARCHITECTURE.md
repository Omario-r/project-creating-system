# ARCHITECTURE — карта модулей

> Ведёт агент. Обновляется при добавлении/удалении модуля или связи между модулями
> (пункт Definition of Done). Это оглавление проекта: отсюда агент узнаёт, какие
> паспорта читать. Детали — только в паспортах модулей, здесь не дублировать.

## Модули

| Модуль | Путь | Назначение (1 строка) | Зависит от |
|--------|------|----------------------|------------|
| <имя> | `src/<имя>/` | <что делает> | <модули или «—»> |

## Схема связей

<Опционально: текстовая или mermaid-схема, если связей больше 5. Только модули и
направления зависимостей, без деталей.>

## Сквозные соглашения

<То, что касается всех модулей: формат ошибок, соглашения именования, точка входа,
как запускать проект и тесты. Кратко, ссылками где возможно.>
